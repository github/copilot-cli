git checkout -b security-edge-tests && pytest -q && git add tests/test_security_edges.py .github/workflows/security-tests.yml && git commit -m "Add security edge tests and CI" && git push -u origin security-edge-tests
